'use strict';

function pkDecode(value){const text=String(value||'').replace(/-/g,'+').replace(/_/g,'/');const raw=atob(text+'==='.slice((text.length+3)%4));return Uint8Array.from(raw,c=>c.charCodeAt(0));}
function pkEncode(value){const bytes=new Uint8Array(value);let raw='';for(const b of bytes)raw+=String.fromCharCode(b);return btoa(raw).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');}
function normalizePublicKeyOptions(options){const x=JSON.parse(JSON.stringify(options));x.challenge=pkDecode(x.challenge);if(x.user)x.user.id=pkDecode(x.user.id);if(x.allowCredentials)x.allowCredentials=x.allowCredentials.map(c=>({...c,id:pkDecode(c.id)}));return x;}

async function registerAdminPasskey(){
  if(!window.PublicKeyCredential||!navigator.credentials)throw new Error('이 브라우저는 WebAuthn 패스키를 지원하지 않습니다.');
  const begin=await api('/api/production/passkeys/register/begin',{method:'POST',body:{}});
  const credential=await navigator.credentials.create({publicKey:normalizePublicKeyOptions(begin.publicKey)});
  const response=credential.response;
  if(typeof response.getPublicKey!=='function'||!response.getPublicKey())throw new Error('브라우저가 공개키 내보내기를 지원하지 않습니다.');
  const values=await openModal({title:'패스키 이름',message:'이 기기를 구분할 이름을 입력하세요.',fields:[{name:'name',label:'이름',value:'Admin Passkey'}],confirmLabel:'등록'});
  if(!values)return;
  await api('/api/production/passkeys/register/finish',{method:'POST',body:{challengeId:begin.challengeId,credentialId:pkEncode(credential.rawId),clientDataJSON:pkEncode(response.clientDataJSON),authenticatorData:pkEncode(response.getAuthenticatorData()),publicKeySpki:pkEncode(response.getPublicKey()),name:values.name}});
  toast('관리자 패스키가 등록되었습니다.');await renderProductionHardening();
}

async function loginWithPasskey(){
  if(!window.PublicKeyCredential||!navigator.credentials)throw new Error('이 브라우저는 WebAuthn 패스키를 지원하지 않습니다.');
  const begin=await api('/api/passkey/login/begin',{method:'POST',body:{role:loginRole.value}});
  const credential=await navigator.credentials.get({publicKey:normalizePublicKeyOptions(begin.publicKey)});
  const response=credential.response;
  const done=await api('/api/passkey/login/finish',{method:'POST',body:{challengeId:begin.challengeId,credentialId:pkEncode(credential.rawId),clientDataJSON:pkEncode(response.clientDataJSON),authenticatorData:pkEncode(response.authenticatorData),signature:pkEncode(response.signature)}});
  session={role:done.role,csrf:done.csrf,expiresAt:done.expiresAt};showApp();
}

async function renderProductionHardening(){
  if(!roleIsAdmin()){content.innerHTML='<div class="empty">FORBIDDEN</div>';return;}
  const d=await api('/api/production'),c=d.compatibility,t=d.transport,u=d.updates,s=d.slo;
  const devices=c.devices.map(x=>`<tr><td>${badge(x.type)}</td><td class="code">${esc(x.id)}</td><td>${badge(x.online?'ONLINE':'OFFLINE')}</td><td>${esc(x.version||'-')} / P${x.protocol}</td><td>${badge(x.compatible?'MATCHED':'MISMATCH')}</td><td>${x.missing.map(badge).join(' ')||'-'}</td><td class="code">${esc(x.fingerprint)}</td></tr>`).join('');
  const incidents=d.incidents.slice(0,30).map(x=>`<tr><td>${badge(x.severity)}</td><td class="code">${esc(x.id)}</td><td>${esc(x.title)}</td><td>${esc(x.entity)}</td><td>${x.count}</td><td>${badge(x.status)}</td><td>${esc(fmtTime(x.lastAt))}</td><td>${x.status==='OPEN'?`<button data-prod-resolve="${esc(x.id)}">Resolve</button>`:'-'}</td></tr>`).join('');
  const tx=u.transactions.slice(0,30).map(x=>`<tr><td>${esc(x.type)}</td><td class="code">${esc(x.id)}</td><td>${esc(x.channel)}</td><td>${esc(x.version)}</td><td>${badge(x.status)}</td><td>${esc(fmtTime(x.updatedAt))}</td></tr>`).join('');
  const passkeys=d.passkeys.map(x=>`<tr><td>${esc(x.name)}</td><td class="code">${esc(x.id)}</td><td>${esc(fmtTime(x.createdAt))}</td><td>${esc(fmtTime(x.lastUsedAt))}</td><td>${x.revokedAt?badge('REVOKED'):`<button class="danger" data-prod-passkey-revoke="${esc(x.id)}">Revoke</button>`}</td></tr>`).join('');
  const approvals=d.approvals.slice(0,20).map(x=>`<tr><td class="code">${esc(x.ticketId)}</td><td>${esc(x.method)} ${esc(x.pathname)}</td><td>${badge(x.status)}</td><td>${esc(x.requestedBy)}</td><td>${esc(x.approvedBy||'-')}</td><td>${x.status==='PENDING'?`<button data-prod-approval="${esc(x.ticketId)}">2차 승인</button>`:'-'}</td></tr>`).join('');
  const anomalies=d.anomalies.map(x=>`<tr><td>${esc(x.key)}</td><td>${badge(x.severity)}</td><td>${badge(x.status)}</td><td>${x.count}</td><td>${esc(fmtTime(x.lastAt))}</td></tr>`).join('');
  content.innerHTML=`
  <div class="cards production-summary">
    <div class="card"><div class="stat-label">COMPATIBLE</div><div class="stat-value">${c.summary.compatible}/${c.summary.online}</div><div class="stat-sub">online fingerprints</div></div>
    <div class="card"><div class="stat-label">TRANSPORT</div><div class="stat-value compact">${esc(t.actualMode)}</div><div class="stat-sub">${t.tlsConfigured?'certificate loaded':'HMAC authenticated'}</div></div>
    <div class="card"><div class="stat-label">SLO</div><div class="stat-value">${s.ackSuccess.toFixed(2)}%</div><div class="stat-sub">budget ${s.errorBudget.remainingPercent}%</div></div>
    <div class="card"><div class="stat-label">AUDIT CHAIN</div><div class="stat-value compact">${d.audit.lastError?'INVALID':'VERIFIED'}</div><div class="stat-sub">${d.audit.count} events</div></div>
  </div>
  ${!t.tlsConfigured?'<div class="warning-box">Relay TCP는 현재 HMAC 인증 모드입니다. TLS 인증서 파일을 서버 환경에 설정하기 전에는 TLS/Pinning이 활성화된 것으로 표시하지 않습니다.</div>':''}
  <div class="production-grid">
    <div class="section-card"><div class="section-head"><h3>Compatibility / Build Fingerprint</h3><span>${c.summary.mismatch} mismatch</span></div><div class="table-wrap"><table><thead><tr><th>Type</th><th>ID</th><th>Link</th><th>Version</th><th>State</th><th>Missing</th><th>Fingerprint</th></tr></thead><tbody>${devices||'<tr><td colspan="7" class="empty">기기 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>Signed Update Supervisor</h3><button data-prod-rollback>Manual Rollback</button></div><div class="section-body kv"><div>Auto rollback</div><div>${badge(u.policy.autoRollback?'ACTIVE':'OFF')}</div><div>Failure threshold</div><div>${u.policy.failureThresholdPercent}% / min ${u.policy.minimumSamples}</div><div>Startup ACK</div><div>${u.policy.startupAckSeconds}s</div></div><div class="table-wrap compact-table"><table><tbody>${tx||'<tr><td class="empty">트랜잭션 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>Audit / Incident Timeline</h3><button data-prod-audit-verify>Verify Chain</button></div><div class="table-wrap"><table><thead><tr><th>Severity</th><th>ID</th><th>Signal</th><th>Entity</th><th>Count</th><th>Status</th><th>Last</th><th></th></tr></thead><tbody>${incidents||'<tr><td colspan="8" class="empty">장애 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>SLO / Error Budget</h3><span>${badge(s.compliant?'GOOD':'WARNING')}</span></div><div class="section-body metric-row"><div><span>Availability</span><strong>${s.availability}%</strong></div><div><span>ACK Success</span><strong>${s.ackSuccess}%</strong></div><div><span>P95</span><strong>${s.p95Ms}ms</strong></div><div><span>Budget Left</span><strong>${s.errorBudget.remainingPercent}%</strong></div></div></div>
    <div class="section-card"><div class="section-head"><h3>WebAuthn Passkeys</h3><button class="primary" data-prod-passkey-register>패스키 등록</button></div><div class="table-wrap"><table><thead><tr><th>Name</th><th>ID</th><th>Created</th><th>Last used</th><th></th></tr></thead><tbody>${passkeys||'<tr><td colspan="5" class="empty">등록된 패스키 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>Two-person Approval</h3><button data-prod-dual-toggle>${d.dualApprovalRequired?'Disable':'Enable'}</button></div><div class="section-body"><p class="small-note">삭제·복원·서비스 종료·핵심 정책 변경은 서로 다른 관리자 세션의 승인 티켓으로 보호할 수 있습니다.</p></div><div class="table-wrap"><table><thead><tr><th>Ticket</th><th>Operation</th><th>Status</th><th>Requester</th><th>Approver</th><th></th></tr></thead><tbody>${approvals||'<tr><td colspan="6" class="empty">승인 티켓 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>Config Dry Run</h3><button class="primary" data-prod-dry-run>새 계획 검사</button></div><div class="section-body"><div class="kv"><div>Manifest Revision</div><div>${c.manifest.revision}</div><div>Last Operator</div><div>${esc(c.manifest.updatedBy||'-')}</div></div>${c.manifest.lastDryRun?`<div class="warning-box"><strong>${esc(c.manifest.lastDryRun.planId)}</strong><br>${c.manifest.lastDryRun.warnings.map(esc).join('<br>')||'검사 통과'}<br><button data-prod-apply-plan="${esc(c.manifest.lastDryRun.planId)}">Apply Plan</button></div>`:''}</div></div>
    <div class="section-card"><div class="section-head"><h3>Recovery / Diagnostics</h3><div class="actions"><button data-prod-drill>Recovery Drill</button><button data-prod-diag>Device Bundle</button></div></div><div class="section-body kv"><div>Last Drill</div><div>${d.recoveryDrills[0]?badge(d.recoveryDrills[0].ok?'PASS':'FAIL'):'-'}</div><div>Bundles</div><div>${d.diagnosticsBundles.length}</div><div>Restore mode</div><div>ISOLATED VERIFY</div></div></div>
    <div class="section-card"><div class="section-head"><h3>Anomaly Detection</h3><span>15 minute window</span></div><div class="table-wrap"><table><thead><tr><th>Rule</th><th>Severity</th><th>Status</th><th>Count</th><th>Last</th></tr></thead><tbody>${anomalies||'<tr><td colspan="5" class="empty">이상 징후 없음</td></tr>'}</tbody></table></div></div>
    <div class="section-card"><div class="section-head"><h3>Retention / Auto Purge</h3><button data-prod-retention>Apply Now</button></div><div class="section-body kv"><div>Audit</div><div>${d.retention.auditDays}d</div><div>Incidents</div><div>${d.retention.incidentDays}d</div><div>Diagnostics</div><div>${d.retention.diagnosticsDays}d</div><div>Updates</div><div>${d.retention.updateDays}d</div></div></div>
    <div class="section-card"><div class="section-head"><h3>SBOM / Build Integrity</h3><button data-prod-sbom>Generate</button></div><div class="section-body kv"><div>Generated</div><div>${esc(fmtTime(d.supplyChain.generatedAt))}</div><div>Files</div><div>${(d.supplyChain.files||[]).length}</div><div>Source Hash</div><div class="code wrap-code">${esc(d.supplyChain.sourceHash||'-')}</div><div>Violations</div><div>${(d.supplyChain.violations||[]).length}</div></div></div>
    <div class="section-card"><div class="section-head"><h3>Safe Chaos Lab</h3><button data-prod-chaos>Run Simulation</button></div><div class="section-body"><p class="small-note">실기기 연결을 끊지 않는 모델 기반 검증입니다. 운영 데이터와 기기에는 쓰지 않습니다.</p><div class="kv"><div>Last Scenario</div><div>${esc(d.chaosRuns[0]?.scenario||'-')}</div><div>Mode</div><div>SIMULATION ONLY</div></div></div></div>
  </div>`;
}

document.getElementById('passkey-login-btn')?.addEventListener('click',()=>loginWithPasskey().catch(e=>{loginError.textContent=e.message;}));
document.addEventListener('click',async event=>{try{
  if(event.target.closest('[data-prod-passkey-register]'))return await registerAdminPasskey();
  const revoke=event.target.closest('[data-prod-passkey-revoke]');if(revoke){await api('/api/production/passkeys/revoke',{method:'POST',body:{id:revoke.dataset.prodPasskeyRevoke}});return await renderProductionHardening();}
  const resolve=event.target.closest('[data-prod-resolve]');if(resolve){await api('/api/production/incidents/resolve',{method:'POST',body:{id:resolve.dataset.prodResolve}});return await renderProductionHardening();}
  if(event.target.closest('[data-prod-audit-verify]')){const r=await api('/api/production/audit/verify',{method:'POST',body:{}});toast(r.verification.ok?'Audit chain verified':`Audit invalid: ${r.verification.error}`,!r.verification.ok);return await renderProductionHardening();}
  if(event.target.closest('[data-prod-dual-toggle]')){const d=await api('/api/production');await api('/api/production/dual-policy',{method:'POST',body:{required:!d.dualApprovalRequired}});toast('2인 승인 정책이 변경되었습니다.');return await renderProductionHardening();}
  const approve=event.target.closest('[data-prod-approval]');if(approve){await api('/api/production/approvals/approve',{method:'POST',body:{ticketId:approve.dataset.prodApproval}});toast('2차 승인이 완료되었습니다.');return await renderProductionHardening();}
  if(event.target.closest('[data-prod-dry-run]')){const v=await openModal({title:'Configuration Dry Run',fields:[{name:'minProtocolVersion',label:'Minimum Protocol',type:'number',value:'2'},{name:'heartbeatMs',label:'Heartbeat ms',type:'number',value:'10000'}],confirmLabel:'검사'});if(!v)return;const r=await api('/api/production/config/dry-run',{method:'POST',body:{minProtocolVersion:Number(v.minProtocolVersion),heartbeatMs:Number(v.heartbeatMs)}});toast(r.plan.safe?'Dry run 통과':`경고 ${r.plan.warnings.length}건`);return await renderProductionHardening();}
  const apply=event.target.closest('[data-prod-apply-plan]');if(apply){await api('/api/production/config/apply',{method:'POST',body:{planId:apply.dataset.prodApplyPlan}});toast('검증된 설정 계획이 적용되었습니다.');return await renderProductionHardening();}
  if(event.target.closest('[data-prod-drill]')){const r=await api('/api/production/recovery-drill',{method:'POST',body:{}});toast(r.drill.ok?'복구 훈련 PASS':'복구 훈련 FAIL',!r.drill.ok);return await renderProductionHardening();}
  if(event.target.closest('[data-prod-diag]')){const v=await openModal({title:'Device Diagnostics Bundle',fields:[{name:'type',label:'Type',type:'select',value:'CLIENT',options:[{value:'CLIENT',label:'CLIENT'},{value:'SERVER',label:'SERVER'}]},{name:'id',label:'Device ID',value:''}],confirmLabel:'생성'});if(!v)return;const r=await api('/api/production/diagnostics',{method:'POST',body:v}),a=document.createElement('a');a.href=`data:application/json;charset=utf-8,${encodeURIComponent(JSON.stringify(r.bundle.payload,null,2))}`;a.download=`${r.bundle.bundleId}.json`;a.click();toast('비밀값이 제거된 진단 번들을 생성했습니다.');return await renderProductionHardening();}
  if(event.target.closest('[data-prod-retention]')){const r=await api('/api/production/retention/apply',{method:'POST',body:{}});toast(`보존 정책 적용: ${Object.values(r.removed).reduce((a,b)=>a+b,0)}건 정리`);return await renderProductionHardening();}
  if(event.target.closest('[data-prod-sbom]')){await api('/api/production/supply-chain',{method:'POST',body:{}});toast('SBOM 및 소스 해시를 생성했습니다.');return await renderProductionHardening();}
  if(event.target.closest('[data-prod-chaos]')){const v=await openModal({title:'Safe Chaos Simulation',fields:[{name:'scenario',label:'Scenario',type:'select',value:'SERVER_OUTAGE',options:['SERVER_OUTAGE','RELAY_LATENCY','DATABASE_READ_ONLY','AUTH_FLOOD'].map(x=>({value:x,label:x}))}],confirmLabel:'시뮬레이션'});if(!v)return;const r=await api('/api/production/chaos',{method:'POST',body:v});toast(`${r.run.scenario}: PASS`);return await renderProductionHardening();}
  if(event.target.closest('[data-prod-rollback]')){const v=await openModal({title:'Signed Update Rollback',fields:[{name:'type',label:'Type',type:'select',value:'SERVER',options:[{value:'SERVER',label:'SERVER'},{value:'CLIENT',label:'CLIENT'}]},{name:'channel',label:'Channel',type:'select',value:'TEST',options:['TEST','BETA','STABLE'].map(x=>({value:x,label:x}))},{name:'reason',label:'Reason',value:'ADMIN_ROLLBACK'}],danger:true,confirmLabel:'ROLLBACK'});if(!v)return;await api('/api/production/update/rollback',{method:'POST',body:v});toast('서명 업데이트 롤백 정책을 적용했습니다.');return await renderProductionHardening();}
}catch(error){toast(error.message,true);}});
