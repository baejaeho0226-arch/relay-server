PURE TCP RELAY / WEB ADMIN v3.4.0 - SQLite + HA Ready

1. Replace the Railway GitHub repository contents with this folder contents.
2. Keep the repository root as:
   server.js
   package.json
   relay/
   services/
   web/
   public/
   ...
3. Railway Variables must include ADMIN_SECRET.
4. Existing TCP proxy target remains the current Relay PORT.
5. Web Admin domain target remains WEB_ADMIN_PORT (default 8080).
6. Railway runtime must use Node.js 22.13 or newer.
7. Redeploy after upload. Existing JSON data is imported into relay.db.

Expected startup log:
Web Admin: v3.4.0
Storage: SQLITE
Device Extensions: ENABLED (CAPABILITIES / DEVICE_INFO / HMAC / CONFIG / SEQUENCE)

Expected WinSockServer after redeploy:
Relay Extensions: SUPPORTED

This build accepts:
CAPABILITIES
DEVICE_INFO
PROTOCOL_PROFILE
DEVICE_SECRET_ACK
DEVICE_AUTH
COMMAND_ACK
DIAGNOSTICS
CONFIG_ACK
PROCESSOR_CONFIG_ACK (server)
UI_STATE (client)

Optional PWA Push variables:
VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY
VAPID_SUBJECT

Optional Daily Health variables:
DAILY_REPORT_TIMEZONE=Asia/Seoul
DAILY_REPORT_RETENTION_DAYS=365

Optional Relay A/B variables (configure reciprocal values on both services):
HA_ENABLED=1
HA_INSTANCE_ID=relay-a
HA_PRIORITY=200
HA_PEER_URL=https://relay-b-admin.example.com
HA_SHARED_SECRET=<same random 32+ character secret>
HA_POLL_MS=2000
HA_FAILOVER_TIMEOUT_MS=10000

Protocol remains 2. Delphi app version is 2.2.0.
