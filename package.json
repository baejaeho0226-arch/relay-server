{
  "name": "pure-tcp-relay",
  "version": "5.1.0",
  "description": "Pure TCP Relay vNext with persistent identities, primary/backup binding, offline queue, replay and DLQ",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-modules.js",
    "test:roadmap14": "node scripts/test-roadmap11-14.js",
    "loadtest": "node tools/load-simulator.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "optionalDependencies": {
    "geoip-lite": "^1.4.10"
  }
}
